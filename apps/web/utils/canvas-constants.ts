export const MIN_POINTS_TO_STOP = 50; // minimum points before we allow axis stop
export const PROXIMITY_THRESHOLD = 50; // distance from center to show warning
export const AXIS_STOP_THRESHOLD = 0.05; // radians (small angle)
export const DIRECTION_SETTLE_POINTS = 10; // points needed to determine direction
export const MIN_DISTANCE_THRESHOLD = 4; // pixels between points
