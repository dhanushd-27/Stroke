export const VERTEX_RADIUS = 15;
export const LINE_WIDTH = 2;
export const TRIANGLE_SIZE = 300; // Side length of the equilateral triangle
export const SNAP_DISTANCE = 20; // Distance to snap to a vertex

// Scoring weights
export const STRAIGHTNESS_WEIGHT = 0.6;
export const ANGLE_WEIGHT = 0.4;

// Thresholds
export const MIN_EDGE_LENGTH = 20; // Minimum length to consider an edge valid for analysis
export const MAX_DEVIATION_THRESHOLD = 30; // Max pixels deviation allowed before penalty
